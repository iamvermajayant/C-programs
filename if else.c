#include<stdio.h>
#include<conio.h>
main()
{
	int number;
	printf("enter any two digit number");
	scanf("%d",&number);
	if (number<=99)
	printf("the number is of two digit");
	else
	printf("the no is not of two digit");
	getch();
	return 0;
	
	
	
	
	
}
